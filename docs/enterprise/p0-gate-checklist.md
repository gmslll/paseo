# Enterprise P0 security gate evidence

This checklist mirrors `ENTERPRISE_IMPLEMENTATION_MASTER.md` section 22.1. It records discoverable
evidence and release gaps. `EVIDENCE` means only that an accepted evidence-shaped test exists; it
does not mean the end-to-end case passes. `MISSING_CALL_SITE` means the production boundary is not
yet proven to invoke the tested policy. `RED` is a known release blocker or missing mandatory real
run. Do not replace these labels with `PASS` without attaching raw release-run evidence.

Current count: `EVIDENCE` 2 (cases 6 and 7), `MISSING_CALL_SITE` 16, and `RED` 2 (cases 9 and 20).
The W7 audit storage candidate has real Darwin dirfd-relative race coverage in
`darwin-audit-file-system.test.ts`; packaged-runtime build, tracing, and desktop-unpack evidence
remain separate release gates.

| Case | Required behavior                                                                                                 | Current state                    | Existing evidence or missing boundary                                                                                                                                                                                                           |
| ---: | ----------------------------------------------------------------------------------------------------------------- | -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|    1 | Employee A cannot list, read, update, or archive employee B's Workspace or Agent.                                 | `MISSING_CALL_SITE`              | `enterprise/access/resource-authorization.test.ts` and `entry-inventory.test.ts` cover the typed authorization inventory; no real-daemon two-Principal resource flow is recorded.                                                               |
|    2 | Guessed foreign `workspaceId`, `agentId`, and `browserId` receive one non-enumerating denial.                     | `MISSING_CALL_SITE`              | Resource authorization and terminal policy tests cover uniform typed denials; the three production RPC families have no combined real-daemon evidence.                                                                                          |
|    3 | After A transfers a subscribed resource to B, A receives no later event.                                          | `MISSING_CALL_SITE`              | Session authority-receipt and Agent-context registry tests exist; no ownership-transfer subscription E2E proves outbound revocation.                                                                                                            |
|    4 | Timeline tail/before/after/gap recovery never exposes B's messages to A.                                          | `MISSING_CALL_SITE`              | Timeline pagination helpers exercise directions without a two-Principal enterprise daemon boundary.                                                                                                                                             |
|    5 | Search, provider recents, child Agents, and Tombstones reveal nothing about B.                                    | `MISSING_CALL_SITE`              | The authorization inventory names resource boundaries; no adversarial E2E covers all four inference surfaces.                                                                                                                                   |
|    6 | Download tokens are Principal/Workspace-bound, expire, and are single-use.                                        | `EVIDENCE`                       | W7's real Darwin `createPaseoDaemon` + public `/api/files/download` fetch proves Principal/Workspace binding, one-use, replay/closed rejection, wrong-PAT denial, and zero-read failures; the policy tests cover expiry and concurrent consume. |
|    7 | `..`, symlinks, and missing descendants cannot escape Workspace Root.                                             | `EVIDENCE`                       | W7's real Darwin ownership cross-flow and W5 safe-FS tests cover dirfd-relative traversal, symlink/missing-descendant denial, and the public Session/HTTP path. Packaged-runtime evidence remains a separate release gate.                      |
|    8 | Logout A then login B on one client exposes no A cache, drafts, attachments, or tabs.                             | `MISSING_CALL_SITE`              | Principal-partitioned client lifecycle/store tests exist; there is no browser E2E covering the complete logout/login residue set.                                                                                                               |
|    9 | Two Browser Profiles isolate Cookie/LocalStorage and persist independently across restart.                        | `RED`                            | Profile registry and desktop feature tests exist. The required real-Electron enterprise Profile isolation/restart harness has no recorded run.                                                                                                  |
|   10 | Concurrent writers for one Profile yield one lease and visible waiting in the original Session.                   | `EVIDENCE` + `MISSING_CALL_SITE` | `enterprise/browser/lease-manager.test.ts` covers typed single-winner/FIFO behavior. Broker wiring and user-visible waiting need real daemon/Electron evidence.                                                                                 |
|   11 | Different Profiles run concurrently without Browser ID misrouting.                                                | `MISSING_CALL_SITE`              | Profile/lease adapters exist; no real-Electron multi-Profile routing case is recorded.                                                                                                                                                          |
|   12 | Browser Host/daemon restart does not revive an old lease.                                                         | `EVIDENCE` + `MISSING_CALL_SITE` | Lease restore/revocation tests exercise the manager contract. No real process-restart boundary is recorded.                                                                                                                                     |
|   13 | A page-account/Profile mismatch stops high-risk actions.                                                          | `MISSING_CALL_SITE`              | Browser binding/profile contracts exist; no production risk-check call site plus real page-account test is recorded.                                                                                                                            |
|   14 | Employees cannot read daemon config, Grants, plugin/provider credentials, or global Terminal.                     | `EVIDENCE` + `MISSING_CALL_SITE` | `enterprise/runtime/terminal-authorization-policy.test.ts` covers global Terminal denial and filtered listing. The other global surfaces lack a single adversarial real-daemon case.                                                            |
|   15 | Company Codex/Claude tokens never enter protocol, snapshots, logs, audit, or client cache.                        | `MISSING_CALL_SITE`              | `local-audit-sink.test.ts` covers audit metadata allowlisting/canaries. No cross-surface secret-canary E2E covers every required output.                                                                                                        |
|   16 | Boss sees employee metadata but cannot read content without a content Grant.                                      | `MISSING_CALL_SITE`              | Boss projection/store and authorization tests exist; no real-daemon projection/content denial flow is recorded.                                                                                                                                 |
|   17 | Boss with a content Grant reads content and creates an audit event.                                               | `MISSING_CALL_SITE`              | Grant/authorization and required audit contracts exist independently. Their production read call site is not wired to a release-ready audit adapter.                                                                                            |
|   18 | Platform admin manages bindings but cannot read content by default.                                               | `MISSING_CALL_SITE`              | Identity and UI projection tests exist; no real-daemon binding-management/content-denial evidence is recorded.                                                                                                                                  |
|   19 | Every break-glass Owner use creates a high-priority audit event.                                                  | `EVIDENCE` + `MISSING_CALL_SITE` | `enterprise/identity/authenticator.test.ts` verifies the typed audit call. Bootstrap still needs the release-ready Darwin audit adapter and real authentication-path evidence.                                                                  |
|   20 | Ten simulated employees chat concurrently for 30 minutes without cross-Principal data or unbounded daemon memory. | `RED`                            | ADR 0009 requires both a 30-minute real-WebSocket daemon-core run and a 30-minute representative real Codex/Claude run. Neither raw run artifact is recorded; the separate three-Profile Electron capacity run is also missing.                 |

## W7 audit-storage production gate

- `EVIDENCE`: portable sink/storage fault matrix, canonicalization, restart poison, queue/close,
  required durability, and observer transitions in `local-audit-sink.test.ts`.
- `EVIDENCE`: Darwin universal Mach-O N-API build plus real rename/symlink replacement tests in
  `darwin-audit-file-system.test.ts`; all child open/read/write/truncate/rename/unlink/enumeration and
  parent fsync operations remain anchored to the validated directory descriptor.
- `RED`: daemon tracing, npm package inspection, Electron ASAR unpack, macOS CI, and packaged-runtime
  binding-load evidence are not yet connected to a release artifact.
- `EVIDENCE`: enterprise bootstrap and the public download path fail closed when the production
  provider is unavailable or not release-ready; packaged Darwin audit/provider loading still needs
  a release-run artifact.
